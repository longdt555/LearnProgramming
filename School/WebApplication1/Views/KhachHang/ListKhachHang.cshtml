@model StoreManagement.Dtos.Respones.SearchResult<KhachHangModel>

@{
    ViewData["Title"] = "KhachHang";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var paging = Tuple.Create(Model.TotalRecords, Model.PageSize, "KhachHang");
    var currentPage = Model.CurrentPage;
    var pageSize = Model.PageSize;
    var data = Model.Data.ToList();
}
<div class="row">
    <div class="col-md-12">
        <div class="card" style="margin-bottom: 11px;">
            <div class="card-header">
                <div class="row">
                    <form class="update col-md-2" action="https://localhost:44319/KhachHang/Add">
                        <button type="submit" class="btn btn-primary btn-round">Thêm mới</button>
                    </form>
                    <div class="col-md-10">
                        <form style="width: 300px; padding-top: 11px; " class="pull-right">
                            <div class="form-group form-inline">
                                <input placeholder="Nhập từ khóa" type="text" name="searchkey" id="search-khachhang" onchange="search(event)" class="form-control form-inline" />
                                <input type="submit" name="submit" value="Tìm Kiếm" class="from-control" onclick="search(event)" id="btn-search" />
                            </div>
                        </form>
                    </div>
                </div>
            </div>
            <div class="card-body">
                <table class="thead-content">
                    <thead class=" text-primary">
                        <tr>
                            <th class="w-5p">
                                STT
                            </th>
                            <th class="w-20p">
                                Họ Tên
                            </th>
                            <th class="w-20p">
                                Email
                            </th>
                            <th class="w-15p">
                                Ngày Lập
                            </th>
                            <th class="w-10p">
                                Đang Hoạt Động
                            </th>
                            <th class="w-15p">
                                Nhận Quảng Cáo
                            </th>
                            <th class="w-6p">Thao tác</th>
                        </tr>
                </table>
              
                <div class="table-responsive" id="khachhang-list">
                    @await Html.PartialAsync("_ListPartial",data)
                </div>

            </div>
        </div>
    </div>
</div>

<div style="margin: 0 auto">

    @await Html.PartialAsync("Pagination", paging)
</div>
<script src="~/js/app/khachhang.js"></script>

<script>
    var currentPage = @currentPage;
    var pageSize = @pageSize;
</script>
